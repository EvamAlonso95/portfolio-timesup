# Project Conventions

- **Package manager**: Use `pnpm` for all dependency operations. Never use `npm`.
- **Build**: `pnpm run build` (runs `tsc -b && vite build`).
- **Dev server**: `pnpm run dev`.
